
  # Design Angular SPA for SimRule

  This is a code bundle for Design Angular SPA for SimRule. The original project is available at https://www.figma.com/design/Kt6Etlp8VeNIkRLXjhMcQb/Design-Angular-SPA-for-SimRule.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  